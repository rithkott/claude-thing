node_modules
dist/
.cache/
.state/
logs/
*.pid
.DS_Store

# emulator working state
emulator/.cache/
emulator/logs/

# scratch copy of nocturne-connector that build-connector-dmg.sh patches
build/

# firmware images are ~350-450MB — never commit them
*.zip
*.ext2
*.dump
# Nocturne 4.1 flashthing members: a whole-disk .wic and the carved partitions
*.wic
*.ext4
*.bin

# feature worktrees live inside the repo but are their own checkouts
.claude/worktrees/

# GitNexus knowledge graph index — local only, rebuilt with `node .gitnexus/run.cjs analyze`
.gitnexus/

# GitNexus generated context + skills — local only, regenerated by `analyze`
/AGENTS.md
.claude/skills/gitnexus/
