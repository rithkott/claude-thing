node_modules/
dist/
.cache/
logs/
*.pid
.DS_Store

# emulator working state
emulator/.cache/
emulator/logs/

# scratch copy of nocturne-connector that build-connector-dmg.sh patches
build/

# firmware images are ~350MB — never commit them
*.zip
*.ext2
*.dump
